# This helper script was created during finalisation to merge the exports
# block into the creative_manifest.json. It was ultimately not executed
# because the manifest was rewritten directly via the agent's Write tool.
# Kept for reference in case future runs prefer the script-based flow.
#
# Status: NOT EXECUTED. The manifest was finalised via direct write.
# Safe to delete.
